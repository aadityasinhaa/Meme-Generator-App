# Meme-Generator-App
A zero-dependency browser-based meme generator. Upload an image, add draggable top/bottom text with classic meme styling, draw annotations (freehand, shapes, eraser), customize fonts and colors, then export as PNG. Single HTML file, no build step, no frameworks — just open and create.
